name: Windows Server (AnyDesk + Wallpaper)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Maximize Performance & Strip Bloatware
        shell: powershell
        run: |
          Write-Host "Aplicando otimizações extremas..."
          
          # Força o plano de energia de Alto Desempenho
          powercfg -s 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          
          # Desativa efeitos visuais e animações
          Set-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" -Name "VisualFXSetting" -Value 2
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name "UserPreferencesMask" -Value ([byte[]](0x90,0x12,0x03,0x80,0x10,0x00,0x00,0x00))
          
          # Desativa o Windows Defender
          try { Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction SilentlyContinue } catch {}
          
          # Desativa serviço SysMain
          Stop-Service -Name "SysMain" -Force -ErrorAction SilentlyContinue
          Set-Service -Name "SysMain" -StartupType Disabled -ErrorAction SilentlyContinue

      - name: Set Custom Anime Wallpaper from Web
        shell: powershell
        run: |
          Write-Host "Baixando e aplicando o wallpaper do Discord..."
          $wallpaperPath = "$env:TEMP\wallpaper.jpg"
          
          $imageUrl = "https://cdn.discordapp.com/attachments/1188762382697234534/1545968092524445797/thumb-1920-1399041.jpg?ex=6a9e127b&is=6a9cc0fb&hm=be14238b51fbbb774deb71e685612af3b45012be03cd9693b2b797f0c7a1969c&"
          
          try {
              Invoke-WebRequest -Uri $imageUrl -OutFile $wallpaperPath
              
              Add-Type -TypeDefinition @"
              using System;
              using System.Runtime.InteropServices;
              public class Wallpaper {
                  [DllImport("user32.dll", CharSet = CharSet.Auto)]
                  public static extern int SystemParametersInfo(int uAction, int uParam, string lpvParam, int fuWinIni);
              }
              "@
              [Wallpaper]::SystemParametersInfo(20, 0, $wallpaperPath, 3)
              Write-Host "Wallpaper aplicado com sucesso!"
          } catch {
              Write-Host "Erro ao baixar o wallpaper. Verifique o link."
          }

      - name: Install Firefox & Clean Desktop (Home Screen)
        shell: powershell
        run: |
          Write-Host "Instalando o Firefox..."
          winget install Mozilla.Firefox --silent --accept-package-agreements --accept-source-agreements
          
          # Limpa completamente a Área de Trabalho
          $publicDesktop = [Environment]::GetFolderPath('CommonDesktopDirectory')
          $userDesktop = [Environment]::GetFolderPath('Desktop')
          
          Remove-Item "$publicDesktop\*" -Force -Recurse -ErrorAction SilentlyContinue
          Remove-Item "$userDesktop\*" -Force -Recurse -ErrorAction SilentlyContinue
          
          # Deixa apenas o Firefox na Home Screen
          $firefoxPath = "C:\Program Files\Mozilla Firefox\firefox.exe"
          if (Test-Path $firefoxPath) {
              $WshShell = New-Object -ComObject WScript.Shell
              $Shortcut = $WshShell.CreateShortcut("$userDesktop\Firefox.lnk")
              $Shortcut.TargetPath = $firefoxPath
              $Shortcut.Save()
          }

      - name: Install & Configure AnyDesk
        shell: powershell
        run: |
          Write-Host "Baixando e instalando o AnyDesk..."
          $url = "https://download.anydesk.com/AnyDesk.exe"
          Invoke-WebRequest -Uri $url -OutFile "AnyDesk.exe"
          
          Start-Process -FilePath "AnyDesk.exe" -ArgumentList '--install "C:\Program Files (x86)\AnyDesk" --start-with-win --create-shortcuts --create-desktop-icon --silent' -Wait
          Start-Sleep -Seconds 5

          $anydeskPath = "C:\Program Files (x86)\AnyDesk\AnyDesk.exe"
          if (Test-Path $anydeskPath) {
              Write-Host "Configurando senha de acesso..."
              $senha = "CloudGaming2026#"
              cmd /c "echo $senha | `"$anydeskPath`" --set-password"
              Start-Sleep -Seconds 3
              
              $id = cmd /c "`"$anydeskPath`" --get-id"
              
              Write-Host "===================================================="
              Write-Host "   WINDOWS SERVER IS READY!                         "
              Write-Host "===================================================="
              Write-Host " ANYDESK ID  : $id"
              Write-Host " SENHA       : CloudGaming2026#"
              Write-Host "===================================================="
          }

      - name: Keep Alive Loop
        shell: powershell
        run: |
          $mins = 350
          for ($i = 1; $i -le $mins; $i++) {
              Start-Sleep -Seconds 60
          }
          
